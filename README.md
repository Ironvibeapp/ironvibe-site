# ironvibe-site

Статический лендинг лежит **в корне репозитория**: каталоги `en/`, `ru/`, … (`index.html` в корне — редирект по языку браузера). Раньше то же дублировалось в `web_landing/`; дубликат удалён, чтобы правки совпадали с тем, что отдаёт GitHub Pages.